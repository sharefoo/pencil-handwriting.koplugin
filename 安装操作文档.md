# pencil-handwriting.koplugin 安装与操作文档

> 版本：0.6.0
> 仓库地址：https://github.com/sharefoo/pencil-handwriting.koplugin
> 适用设备：带 Wacom/EMR 数字笔的墨水屏设备（Kindle Scribe / Scribe 3 / Colorsoft）

---

## 一、这个插件做什么

在 KOReader 阅读界面里直接用数字笔在页面上手写。笔迹画在当前页面上并随页面刷新保留，
翻页、开菜单、旋转、擦除之后都会自动补画回来，不会丢失。

笔记按页保存在文档的 sidecar 目录中，重新打开文档自动恢复。

---

## 二、前置条件

| 项目 | 要求 | 说明 |
|---|---|---|
| 设备 | 带 Wacom/EMR 数字笔的机型 | Kindle Scribe 系列。普通 Kindle 无数字笔，不适用 |
| KOReader | 已安装并可正常运行 | Kindle 需先越狱。越狱与安装 KOReader 不在本文档范围内 |
| KOReader 版本 | 不限 | 新版走官方 stylus API；旧版自动回退到 evdev 直读路径 |

> 关于输入路径：新版 KOReader（nightly 2026.07.2-60 及以后）提供 stylus 回调 API，
> 插件优先走这条路径，笔事件可「独占」通道，笔画不会被误判成翻页手势。
> 旧版没有该 API，插件会自动改为直接读取数字笔的 evdev 节点，功能可用，但手势隔离效果略弱。

---

## 三、安装

### 步骤 1：获取插件文件

**方式 A：下载 ZIP（推荐）**

1. 打开 https://github.com/sharefoo/pencil-handwriting.koplugin
2. 点击绿色 `Code` 按钮 → `Download ZIP`
3. 解压，得到 `pencil-handwriting.koplugin` 文件夹

**方式 B：git clone**

```bash
git clone https://github.com/sharefoo/pencil-handwriting.koplugin.git
```

> 如果 clone 失败并提示 `Connection was reset`，说明需要给 git 配代理，参见
> 本工作区的 `git环境配置速查.md`。

### 步骤 2：确认文件夹结构

解压后目录应当长这样，**`_meta.lua` 和 `main.lua` 必须直接位于 `pencil-handwriting.koplugin` 文件夹内**：

```
pencil-handwriting.koplugin/
├── _meta.lua
├── main.lua
├── README.md
└── pencilhw/
    ├── canvas.lua
    ├── config.lua
    ├── evdev.lua
    └── store.lua
```

> 常见错误：解压后多套了一层同名目录（如 `pencil-handwriting.koplugin-main/pencil-handwriting.koplugin/`）。
> 复制时务必复制**最内层**那个包含 `_meta.lua` 的文件夹。

### 步骤 3：在设备上完全退出 KOReader

**这一步不能跳过。** KOReader 运行时会持有插件目录，此时改动文件可能导致插件加载异常。

在 Kindle 上退出 KOReader，回到 KUAL 或系统界面。

### 步骤 4：USB 连接电脑

1. 用数据线连接 Kindle 与电脑
2. Kindle 屏幕上会提示连接方式，选择**文件传输 / File transfer**（不要选充电）
3. 电脑上会出现 Kindle 的可移动磁盘

### 步骤 5：复制插件

1. 打开 Kindle 磁盘
2. 进入 `koreader` 目录，再进入 `plugins` 目录
   - 设备内对应路径为 `/mnt/us/koreader/plugins/`
   - 若磁盘根目录下没有 `koreader` 目录，请先确认 KOReader 是否装在别的位置
3. 把 `pencil-handwriting.koplugin` **整个文件夹**粘贴进去

复制完成后的结构应为：

```
Kindle 磁盘/
└── koreader/
    └── plugins/
        └── pencil-handwriting.koplugin/
            ├── _meta.lua
            ├── main.lua
            └── pencilhw/
```

> 文件夹名可以改，但**必须以 `.koplugin` 结尾**，否则 KOReader 不会把它识别为插件。

### 步骤 6：安全弹出并启动

1. 在电脑上「安全弹出 / 弹出设备」
2. 拔掉数据线
3. 在 Kindle 上启动 KOReader

---

## 四、验证安装是否成功

1. 在 KOReader 中**打开任意一本书 / 文档**（必须是文档界面，文件管理器里看不到此插件）
2. 调出主菜单
3. 找到 **排版**（Typeset）标签页
4. 应当能看到 **Pencil handwriting** 一项

> 不同的 KOReader 版本与语言设置下，标签页名称略有差异。若在「排版」里找不到，
> 可逐个标签页翻一下，或按第五节排查。

---

## 五、基本操作

进入 **主菜单 → 排版 → Pencil handwriting**，菜单项如下。

### 5.1 开关与笔触样式

| 菜单项 | 说明 |
|---|---|
| **Enable drawing** | 绘制总开关。开启后笔可直接书写；关闭后笔不再产生新笔画 |
| **Width** | 笔宽，预设 8 档：1 / 2 / 3 / 5 / 8 / 12 / 16 / 24（默认 3）。选 `Custom...` 可填 1–24 之间任意整数 |
| **Color** | 笔画灰度：Black / Gray 25% / Gray 50% / Gray 75% / White（默认 Black） |

> **重要**：关闭「Enable drawing」只是停止书写，**已存的笔记依然显示在页面上**。
> 不需要笔记时再用下方的清除功能。这个设计是为了让你能随时空出手指翻页、点菜单。

### 5.2 书写与擦除

| 操作 | 方式 |
|---|---|
| 写字 | 开启绘制后，直接用笔在页面上写 |
| 擦除 | 用 Premium Pen 的笔尾橡皮，或切换为橡皮工具后在笔画上划 |
| 翻页 | 用**指尖**滑动。绘制开启时笔不建议用于翻页（见 6.4） |
| 点菜单 | 用**指尖**。绘制开启时笔事件被插件接管，笔点不动菜单 |

> **橡皮擦的语义**：本插件只能「加墨」，不能「涂白」（涂白会盖住页面正文）。
> 因此橡皮是**划到哪里，哪条笔画整体消失**，而不是局部裁剪。

### 5.3 笔画管理

| 菜单项 | 说明 |
|---|---|
| **Redraw strokes** | 从存储数据强制重画当前页笔画。界面显示异常时可用 |
| **Clear strokes on this page** | 清除当前页所有笔画（会先弹窗确认并显示数量） |
| **Clear all strokes in document** | 清除整本文档所有笔画（会先弹窗确认并显示总数） |

### 5.4 用「绘制开关」绑定手势

插件向 KOReader 的 Dispatcher 注册了一个动作：

- 动作名：**Pencil handwriting: toggle**

可在 **主菜单 → 工具 → 手势管理**（不同版本路径略有差异）中，把它绑定到手势上，
例如双指长按切换绘制。这样不用每次进菜单。

---

## 六、进阶设置

### 6.1 Input source（输入源）

| 选项 | 行为 | 何时用 |
|---|---|---|
| **Auto（推荐）** | 优先用 KOReader 的 stylus API，不可用时回退到 evdev 直读 | 默认，绝大多数情况 |
| **KOReader stylus API** | 只用官方 stylus 回调 | 已确认设备有该 API 时，手势隔离效果最好 |
| **evdev (direct)** | 只由插件自己读数字笔节点 | 旧版 KOReader，或官方 API 行为异常时 |

若选了 `KOReader stylus API` 但当前 KOReader 构建没有该 API，开启绘制时会弹窗提示，
此时切回 `Auto` 或 `evdev (direct)` 即可。

### 6.2 Exclusive pen capture（独占捕获笔节点）

**仅在 evdev 直读路径下生效。** 开启后插件独占数字笔节点，KOReader 的手势检测器看不到笔事件，
从而彻底避免「写字被识别成翻页」。

> **风险提示**：如果该设备上数字笔与触摸屏共用同一个输入节点，开启独占会让**手指触摸也失效**。
> Kindle Scribe 的笔有独立节点，可以安全开启。开启时会弹窗二次确认，务必先看清说明。
> 若开启后手指失灵，用笔关掉这一项即可恢复。

建议：**先跑 Auto 模式测试。只有在确认写字会误翻页时，才开启此选项。**

### 6.3 Block touch while drawing（绘制时屏蔽触摸）

开启后，绘制状态下忽略触摸手势，手掌或手腕压在屏幕上不会翻页。

- **顶部条带保留可点**，所以菜单始终能打开，不会把自己锁死
- 默认**关闭**——因为笔事件本身已被过滤，屏蔽全部触摸属于更强的干预

### 6.4 关于「笔不能点菜单」

绘制开启时，笔事件被插件接管用于书写，属于设计行为而非缺陷。
需要操作菜单时用指尖，或先用 Dispatcher 手势关掉绘制。

---

## 七、数据存储与备份

### 7.1 数据存在哪里

笔画保存在文档的 **sidecar 目录**内，文件名为：

```
pencil_handwriting.lua
```

sidecar 目录通常是**与书籍同目录、以书名 + `.sdr` 结尾**的文件夹，例如：

```
books/
├── 某文档.pdf
└── 某文档.sdr/
    ├── metadata.pdf.lua
    ├── pencil_handwriting.lua     <-- 本插件的笔记数据
    └── ...
```

### 7.2 备份

备份笔记 = 备份这个 `pencil_handwriting.lua` 文件。
把它复制走，换设备或重装后放回同一个 `.sdr` 目录即可恢复。

### 7.3 注意事项

- 该文件由插件自动读写，**不要在设备运行时手工编辑**
- 文件内带格式版本号，跨大版本升级时若格式变更，旧数据可能无法读取
- 删除 `.sdr` 目录会同时清掉笔记、阅读进度、书签等所有附属数据，请谨慎

---

## 八、故障排查

先进入 **主菜单 → 排版 → Pencil handwriting → Input diagnostics**，
这个面板会列出：当前构建、绘制状态、输入源与实际生效的通道、KOReader stylus API 是否存在、
重绘钩子是否安装、页面键值、笔画数量、sidecar 路径、各类计数器、evdev 节点与坐标范围、
以及设备上所有输入设备的清单。**绝大多数问题看这里就能定位。**

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 菜单里找不到 Pencil handwriting | 在文件管理器里而非文档界面 | 先打开一本书，再调出主菜单 |
| 同上 | 插件没装到正确位置 | 确认路径为 `koreader/plugins/pencil-handwriting.koplugin/_meta.lua` |
| 同上 | 文件夹名不以 `.koplugin` 结尾 | 改名，补上 `.koplugin` 后缀 |
| 同上 | 安装时 KOReader 未退出，插件未重载 | 完全退出 KOReader 后重新启动 |
| 插件加载失败并弹窗报错 | `pencilhw/` 子目录缺失或损坏 | 重新完整复制整个文件夹 |
| 开启了绘制但写不出笔画 | 输入源选错 | 把 Input source 切到 `Auto` |
| 同上 | 未找到数字笔节点 | 看诊断面板的「Input devices」列表；若节点存在但未被选中，见下方「手动指定节点」 |
| 诊断面板显示 `stylus API: missing` 且输入源为 stylus | 当前 KOReader 构建无该 API | 切到 `Auto` 或 `evdev (direct)` |
| 写字时频繁翻页 | 笔事件被手势检测器读到 | 输入源切 `Auto`（走 stylus API）；若无效，开启 `Exclusive pen capture` |
| 笔画在翻页后消失 | 重绘钩子未安装 | 看诊断面板 `Repaint hook` 是否为 `MISSING`；查看日志确认 `ReaderView.paintTo` 是否可用 |
| 界面出现白屏 | 旧版本遗留问题，0.6.0 已改为纯加墨渲染 | 确认版本为 0.6.0；仍复现请提 issue 并附诊断面板内容 |
| 笔迹位置整体偏移、缩放不对 | 数字笔上报的是原始坐标，轴范围识别有误 | 诊断面板查看 `raw range` 是否合理；evdev 路径下由插件自动按轴范围缩放 |
| 屏幕出现残影 | 快刷累积的鬼影 | 抬笔后插件会自动整屏刷新一次；长笔画中每几秒也会刷一次 |
| 批注多时翻页变慢 | 笔画数据量大，渲染与擦除扫描开销上升 | 分页清理，或用 `Clear all strokes in document` 清理 |
| EPUB 重排后笔迹错位 | 流式文档排版变化，笔画锚点失效 | 已知限制，建议用 PDF 做批注 |

### 手动指定数字笔节点（进阶）

插件默认按 `BTN_TOOL_PEN` 能力自动定位数字笔节点。
若自动识别选错了节点，可编辑 `pencilhw/config.lua`，把节点路径填进：

```lua
Config.EXTRA_DEVICE_PATHS = { "/dev/input/event3" }
```

该列表会被**优先尝试**，填错才会回退到自动识别。
节点名与索引因机型、固件版本而异，具体路径请从诊断面板的设备列表里抄。

---

## 九、卸载

1. 在设备上完全退出 KOReader
2. USB 连接电脑，删除 `koreader/plugins/pencil-handwriting.koplugin` 整个文件夹
3. 安全弹出，重启 KOReader

**笔记数据不会随插件删除**。若也要清掉，需另行删除各本书 `.sdr` 目录下的
`pencil_handwriting.lua`。

---

## 十、已知限制

- 不支持逐笔选中、改色、移动
- 橡皮擦为整笔删除，不能局部裁剪
- 无压感调宽
- 流式文档（EPUB）重排后笔画位置可能漂移
- 绘制开启时笔无法操作菜单，需用指尖
- 墨迹极密的页面，翻页会变慢

---

## 附：给贡献者的开发说明

- 所有子模块位于插件私有的 `pencilhw/` 命名空间下。KOReader 的 `package.loaded`
  是全局共享的，若使用 `core/store` 这类通用名，多个插件同时加载时会互相覆盖，
  因此这里刻意做了命名空间隔离。
- 渲染只在 framebuffer 上**加墨**，从不铺白。早期版本保留整屏白色画布并覆盖 framebuffer，
  结果盖住了页面与所有打开的菜单（表现为白屏），且 KOReader 一重绘笔记就消失。
- 主要配置集中在 `pencilhw/config.lua`，笔宽档位、调色板、橡皮半径、
  刷新节流参数、坐标缩放开关等都在那里。

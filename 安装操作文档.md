# pencil-handwriting.koplugin 安装与操作文档

> **版本**：0.9.9
> **仓库**：<https://github.com/sharefoo/pencil-handwriting.koplugin>
> **适用设备**：带 Wacom/EMR 数字笔的墨水屏设备（Kindle Scribe / Scribe 3 / Colorsoft）

---

## 一、这个插件做什么

在 KOReader 的阅读界面里，直接用数字笔在页面上手写批注。

- 笔迹画在页面上，翻页、开菜单、旋转、擦除之后都会自动补画回来，不会丢失；
- 笔记按文档保存在 sidecar 目录，重新打开自动恢复；
- 笔画锚定在**文档页面坐标**上，PDF 改变缩放、滚动或旋转屏幕后，笔迹跟着内容走，不会跑到别的内容上；
- 关闭「启用手写」只是停止书写，**已写的笔记依然显示**，方便随时用指尖翻页、点菜单。

---

## 二、前置条件

| 项目 | 要求 | 说明 |
|---|---|---|
| 设备 | 带 Wacom/EMR 数字笔的机型 | Kindle Scribe 系列。普通 Kindle 无数字笔，不适用 |
| KOReader | 已安装且能正常运行 | Kindle 需先越狱，越狱与安装 KOReader 不在本文档范围内 |
| KOReader 版本 | **不限** | 新版走官方 stylus API，旧版自动回退到直读数字笔节点 |
| 电脑 | 能通过 USB 读写 Kindle 磁盘 | 用于复制插件文件 |

**关于输入路径**：nightly 2026.07.2-60 及以后的 KOReader 提供 stylus 回调 API，插件优先走这条路径——笔事件可被「独占」，笔画不会被误判成翻页手势。更早的构建没有该 API，插件会自动改为直接读取数字笔的 evdev 节点：功能可用，但手势隔离效果略弱。

> **提示**：建议升级到带该 API 的 nightly 版本再使用，体验差别明显。插件本身在两种版本下都能工作。

---

## 三、安装

### 步骤 1：获取插件文件

**方式 A：下载 ZIP（推荐）**

1. 打开 <https://github.com/sharefoo/pencil-handwriting.koplugin>
2. 点击绿色 `Code` 按钮 → `Download ZIP`
3. 解压，得到 `pencil-handwriting.koplugin` 文件夹

**方式 B：git clone**

```bash
git clone https://github.com/sharefoo/pencil-handwriting.koplugin.git
```

> 若 clone 提示 `Connection was reset` 或超时，通常是网络无法直连 GitHub，
> 需为 git 配置代理后重试（HTTPS 代理即可）。

### 步骤 2：确认文件夹结构（最容易出错的一步）

复制前先确认目录长这样，**`_meta.lua` 与 `main.lua` 必须直接位于 `pencil-handwriting.koplugin` 文件夹内**：

```
pencil-handwriting.koplugin/
├── _meta.lua
├── main.lua
├── README.md
├── 安装操作文档.md
├── assets/
│   └── wechat-donate.jpg   （捐赠收款码，仅文档用；删掉不影响插件运行）
└── pencilhw/
    ├── canvas.lua
    ├── config.lua
    ├── evdev.lua
    ├── geometry.lua
    ├── i18n.lua
    ├── store.lua
    └── viewmap.lua
```

> **两个常见错误**
>
> 1. **多套一层同名目录**：解压后出现 `pencil-handwriting.koplugin-main/pencil-handwriting.koplugin/`。
>    复制时务必取**最内层**那个包含 `_meta.lua` 的文件夹。
> 2. **漏掉 `pencilhw/` 子目录**：只复制了 `_meta.lua` 与 `main.lua`。
>    插件会在打开文档时弹窗提示加载失败——不会静默失效，但功能不可用。

### 步骤 3：在设备上完全退出 KOReader

**这一步不能跳过。** KOReader 运行时会持有插件目录，此时改动文件可能导致插件加载异常。

在 Kindle 上退出 KOReader，回到 KUAL 或系统界面。

### 步骤 4：USB 连接电脑

1. 用数据线连接 Kindle 与电脑
2. Kindle 屏幕上会提示连接方式，选择**文件传输 / File transfer**（不要选充电）
3. 电脑上会出现 Kindle 的可移动磁盘

### 步骤 5：复制插件

1. 打开 Kindle 磁盘
2. 进入 `koreader` 目录，再进入 `plugins` 目录
   - 设备内对应路径为 `/mnt/us/koreader/plugins/`
   - 若磁盘根目录下没有 `koreader` 目录，先确认 KOReader 是否装在别的位置（有些安装方式在 `.adds/koreader/` 下，放同一个位置即可）
3. 把 `pencil-handwriting.koplugin` **整个文件夹**粘贴进去

复制完成后的结构应为：

```
Kindle 磁盘/
└── koreader/
    └── plugins/
        └── pencil-handwriting.koplugin/
            ├── _meta.lua
            ├── main.lua
            └── pencilhw/
                └── ...
```

> 文件夹名可以改，但**必须以 `.koplugin` 结尾**，否则 KOReader 不会把它识别为插件。

### 步骤 6：安全弹出并启动

1. 在电脑上「安全弹出 / 弹出设备」
2. 拔掉数据线
3. 在 Kindle 上启动 KOReader

> **插件目录只在 KOReader 启动时扫描一次**。复制文件后必须**完全退出再重新启动**，
> 仅退回文件管理器不会重新加载插件。

---

## 四、验证安装是否成功

1. 在 KOReader 中**打开任意一本书 / 文档**
   （插件菜单只在**阅读界面**存在，文件管理器里看不到）
2. 调出主菜单
3. 切到 **排版**（Typeset）标签页，也就是**左起第二栏**
4. 应当能看到 **Pencil handwriting** 一项

> 不同 KOReader 版本与界面语言下，标签页名称略有差异。若在「排版」里找不到，
> 逐个标签页翻一下，或按第八节排查。

---

## 五、基本操作

进入 **主菜单 → 排版 → Pencil handwriting**。菜单文字跟随 KOReader 的界面语言：中文界面显示中文，其他语言显示英文原文（插件名 `Pencil handwriting` 保持不变）。

### 5.1 菜单一览

| 菜单项（中文界面） | 英文原文 | 说明 |
|---|---|---|
| 启用手写 | Enable drawing | 绘制总开关。**按文档记忆**，重开同一本书自动恢复上次状态 |
| 笔宽：N | Width: N | 预设 1 / 2 / 3 / 5 / 8 / 12 / 16 / 24（默认 3）。选「自定义…」可填 1–24 之间任意整数 |
| 颜色：X | Color: X | 五档灰度：黑 / 灰 25% / 灰 50% / 灰 75% / 白（默认黑） |
| 输入源：X | Input source: X | 自动 / KOReader stylus API / evdev（直读），见 6.1 |
| 书写时屏蔽触摸 | Block touch while drawing | 见 6.3 |
| 独占笔输入（仅 evdev） | Exclusive pen capture (evdev only) | 见 6.2 |
| 重绘笔迹 | Redraw strokes | 按已存数据强制重画当前页，界面显示异常时用 |
| 清除本页笔迹 | Clear strokes on this page | 弹窗确认并显示本页笔数 |
| 清除本文档所有笔迹 | Clear all strokes in document | 弹窗确认并显示总笔数 |
| 翻页整屏刷新（测试） | Full refresh on page turn (test) | 每次翻页都整屏刷新，用于区分「残影」与「页标识错误」，见 6.4 |
| 输入诊断 | Input diagnostics | 诊断面板，排错必看，见第八节 |

> **关闭「启用手写」不会隐藏笔记**，只是停止书写。要清除笔记请用下面两项「清除」功能。

### 5.2 书写与擦除

| 操作 | 方式 |
|---|---|
| 写字 | 开启绘制后，直接用笔在页面上写 |
| 擦除 | 用 Premium Pen 的笔尾橡皮，或切换为橡皮工具后在笔画上划 |
| 翻页 | 用**指尖**滑动。绘制开启时笔被插件接管，不用笔翻页 |
| 点菜单 | 用**指尖**。绘制开启时笔点不动菜单（见 6.5） |

> **橡皮擦的语义**：插件只能「加墨」，不能「涂白」（涂白会盖住页面正文）。
> 因此橡皮是**划到哪里，哪条笔画整体消失**，而不是局部裁剪。
> 判定用的是「橡皮圆盘到笔画线段的距离」，所以在缩小视图下也能正常擦到。

### 5.3 书写手感

- **落笔即时显墨**：笔尖移动时墨直接写进屏幕缓冲，只刷新笔尖覆盖的矩形，延迟最低。
- **抬笔后自动整屏刷新一次**（约 0.6 秒后），用来消除局部快刷留下的残影。这一步会看到一次完整刷新，属正常现象。
- **翻页时的刷新**：若刚要离开的那一页有墨迹，翻页后插件会补一次整屏刷新——否则上一页的笔迹会以残影形式留在新页上。没写过字的页面仍是正常快速翻页。

### 5.4 用「绘制开关」绑定手势

插件向 KOReader 的 Dispatcher 注册了一个动作：

- 动作名：**Pencil handwriting: toggle**

可在 **主菜单 → 工具 → 手势管理**（不同版本路径略有差异）中把它绑到手势上，
例如双指长按切换绘制，这样不必每次进菜单。

---

## 六、进阶设置

### 6.1 输入源（Input source）

| 选项 | 行为 | 何时用 |
|---|---|---|
| **自动（推荐）** | 优先用 KOReader 的 stylus API，不可用时回退到 evdev 直读 | 默认，绝大多数情况 |
| **KOReader stylus API** | 只用官方 stylus 回调 | 已确认设备有该 API 时，手势隔离效果最好 |
| **evdev（直读）** | 只由插件自己读数字笔节点 | 旧版 KOReader，或官方 API 行为异常时 |

若选了 `KOReader stylus API` 但当前 KOReader 构建没有该 API，开启绘制时会弹窗提示，
此时切回「自动」或「evdev（直读）」即可。切换输入源**立即生效**，不需要重启。

### 6.2 独占笔输入（Exclusive pen capture）

**仅在 evdev 直读路径下生效。** 开启后插件独占数字笔节点，KOReader 的手势检测器看不到笔事件，
从而彻底避免「写字被识别成翻页」。

> **风险提示**：如果该设备上数字笔与触摸屏共用同一个输入节点，开启独占会让**手指触摸也失效**。
> Kindle Scribe 的笔有独立节点，可以安全开启。开启时会弹窗二次确认，务必先看清说明。
> 若开启后手指失灵，用笔关掉这一项即可恢复（或等它随文档关闭而释放）。

**建议**：先跑「自动」模式测试。只有在确认写字会误翻页时，才开启此选项。

### 6.3 书写时屏蔽触摸（Block touch while drawing）

开启后，绘制状态下忽略触摸手势，手掌或手腕压在屏幕上不会翻页。

- **顶部条带保留可点**，所以菜单始终能打开，不会把自己锁死；
- 默认**关闭**——因为笔事件本身已被过滤，屏蔽全部触摸属于更强的干预。

### 6.4 翻页整屏刷新（测试项）

默认**关闭**。打开后每次翻页都整屏刷新一次。

它是用来二分定位「笔迹跟着到下一页」这类问题的：

- **打开后正常了** → 之前是面板没刷干净留下的**残影**，不是数据问题；
- **打开后照样串页** → 是页标识问题，看诊断面板的 `stroke space` 与 `Last stroke`。

### 6.5 关于「笔不能点菜单」

绘制开启时，笔事件被插件接管用于书写，属于设计行为而非缺陷。
这也是「写字不会被识别成翻页」的代价。需要操作菜单时用指尖，或先用 Dispatcher 手势关掉绘制。

---

## 七、数据存储与备份

### 7.1 数据存在哪里

笔画保存在文档的 **sidecar 目录**内，文件名固定为：

```
pencil_handwriting.lua
```

sidecar 目录通常是**与书籍同目录、以书名 + `.sdr` 结尾**的文件夹：

```
books/
├── 某文档.pdf
└── 某文档.sdr/
    ├── metadata.pdf.lua
    ├── pencil_handwriting.lua     <-- 本插件的笔记数据
    └── ...
```

文件名与路径可在诊断面板里直接看到（`sidecar:` 那一行）。

### 7.2 备份与迁移

备份笔记 = 复制这个 `pencil_handwriting.lua` 文件。
换设备或重装后，放回同一个 `.sdr` 目录即可恢复。

### 7.3 注意事项

- 该文件由插件自动读写，**不要在设备运行时手工编辑**；
- 文件内带格式版本号（当前为 **v2**）。**早期版本（v1，屏幕坐标）写下的笔迹在加载时会被跳过**，
  文件保留不动，但那些笔记需要重画——它们在新的坐标体系下无法被正确安放；
- 删除 `.sdr` 目录会同时清掉笔记、阅读进度、书签等所有附属数据，请谨慎；
- 插件按「停笔 / 翻页 / 关闭文档 / 设备休眠」等时机写盘，正常使用无需手动保存。

---

## 八、故障排查

### 8.1 先看诊断面板

进入 **主菜单 → 排版 → Pencil handwriting → 输入诊断**。面板内容较多，一屏放不下，**向下滚动**。
同样的文本也会写入 `koreader/crash.log`（前缀 `PencilHW: diagnostics`），用 USB 复制出来更省事。

重点行及其含意：

| 行 | 含义 |
|---|---|
| `language:` | 当前界面语言与词典状态。显示 `Chinese active` 说明菜单应为中文 |
| `Drawing:` | 绘制开关状态 |
| `Active source:` | 实际生效的输入通道：`stylus` / `evdev` / `registered (idle)` / `none` |
| `KOReader stylus API:` | 当前构建是否有官方手写笔 API |
| `Repaint hook:` | 重绘钩子是否安装。**`MISSING` 时笔迹无法在重绘后恢复** |
| `rotation=` / `Rotation sources:` | 旋转方向与两个来源（输入层旋转、屏幕旋转）的取值 |
| `toolmap:` | 插件认定的笔 / 橡皮 / 高亮工具号。用一次橡皮即可对照 |
| `stroke space:` | 笔画坐标系：`page` 表示锚定在文档页面（正确），`native` 表示锚定在屏幕 |
| `Last stroke:` | 最近一笔写到哪一页、存在哪个坐标系 |
| `Page key:` / `Page accessors:` | 页标识及其来源 |
| `strokes on page / in document / pages stored` | 本页笔数、全文档笔数、占用的页数 |
| `sidecar:` / `file: … saved strokes:` | 数据文件路径、字节数、上次保存笔数、上次错误 |
| 各类计数器 | `stylus slots` / `pen downs` / `live stamps` / `unmapped pages` / `stroke pauses` / `off-panel` / `stylus handler errors` / `erase unmapped` 等，用于定位具体环节 |
| `Input devices on this reader:` | 设备上所有输入节点，带 `[pen-tool]` 标记的是上报数字笔能力的节点 |

### 8.2 症状对照表

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 菜单里找不到 Pencil handwriting | 在文件管理器里而非文档界面 | 先打开一本书，再调出主菜单 |
| 同上 | 插件没装到正确位置 | 确认路径为 `koreader/plugins/pencil-handwriting.koplugin/_meta.lua` |
| 同上 | 文件夹名不以 `.koplugin` 结尾 | 改名，补上 `.koplugin` 后缀 |
| 同上 | 安装时 KOReader 未退出，插件未重载 | 完全退出 KOReader 后重新启动 |
| 插件加载失败并弹窗报错 | `pencilhw/` 子目录缺失或损坏 | 重新完整复制整个文件夹 |
| 开启了绘制但写不出笔画 | 输入源选错 | 把输入源切到「自动」 |
| 同上 | 未找到数字笔节点 | 看诊断面板的 `Input devices` 列表；必要时按 8.3 手动指定节点 |
| 诊断面板显示 stylus API 缺失而输入源为 stylus | 当前 KOReader 构建无该 API | 切到「自动」或「evdev（直读）」 |
| 写字时频繁翻页 | 笔事件被手势检测器读到 | 输入源切「自动」（走 stylus API）；若无效，开启「独占笔输入」 |
| 笔迹在翻页后消失 | 重绘钩子未安装 | 看诊断面板 `Repaint hook` 是否为 `MISSING` |
| 笔迹出现在相邻页 | 页标识问题 | 看 `stroke space`（应为 `page`）与 `Last stroke`；清一次笔记后重测 |
| 旧笔记不见了 | 那是 v1（屏幕坐标）格式的数据 | 需要重画，见 7.3 |
| 橡皮擦没反应 | 擦除位置解析不到页面坐标 | 看诊断面板 `erase unmapped` 计数；同时用橡皮划一下，看 `Last slot` 的 `tool=` 是否等于 `toolmap:` 里的橡皮值 |
| 菜单仍是英文 | 界面语言不是中文 | 看诊断面板 `language:` 一行；本插件按界面语言决定菜单语言 |
| 界面出现白屏 / 笔记被抹掉 | 早期版本遗留问题 | 确认版本为 0.9.x；仍复现请附诊断面板内容提 issue |
| 笔迹位置整体偏移或缩放不对 | 坐标换算与设备不匹配 | 看诊断面板 `Rotation sources` 与 `off-panel` 计数；`map fallbacks` 在涨说明旋转映射被判为不匹配 |
| PDF 缩放、滚动后笔迹错位 | 不该发生（笔画锚定页面坐标） | 看 `stroke space` 是否为 `page`；若为 `native`，说明页面映射不可用，反馈诊断内容 |
| 屏幕出现残影 | 快刷累积的鬼影 | 抬笔后插件会自动整屏刷新；长笔画中每几秒也会刷一次 |
| 翻页时多闪一下 | 「离开有墨页面」时的整屏刷新 | 有意设计，用于清除笔迹残影；没写过字的页面不会闪 |
| 批注多时翻页变慢 | 笔画数据量大，渲染与擦除扫描开销上升 | 分页清理，或用「清除本文档所有笔迹」 |
| EPUB 重排后笔迹错位 | 流式文档没有页面坐标系 | 已知限制，建议用 PDF 做批注 |

### 8.3 手动指定数字笔节点（进阶）

插件默认按 `BTN_TOOL_PEN` / `BTN_TOOL_RUBBER` 能力自动定位数字笔节点。
若自动识别选错了节点，可编辑 `pencilhw/config.lua`：

```lua
Config.EXTRA_DEVICE_PATHS = { "/dev/input/event3" }
```

该列表会被**优先尝试**，都不匹配时才回退到自动识别。
节点名与索引因机型、固件版本而异，具体路径请从诊断面板的设备列表里抄。

### 8.4 收集日志

出现异常时，把 `koreader/crash.log` 里含 `PencilHW` 的行复制出来（诊断面板内容也在其中），
再附上设备型号与 KOReader 版本（**帮助 → 关于**），基本可以定位到具体环节。

---

## 九、升级与卸载

### 9.1 升级插件

1. 完全退出 KOReader；
2. USB 连接电脑，用新版本文件夹**整个替换** `koreader/plugins/pencil-handwriting.koplugin`；
3. 安全弹出，重新启动 KOReader。

笔记数据存放在各书目的 `.sdr` 目录里，**不随插件替换而丢失**。
但若新版本修改了存储格式，旧格式数据可能被跳过（见 7.3）。

### 9.2 卸载

1. 在设备上完全退出 KOReader；
2. USB 连接电脑，删除 `koreader/plugins/pencil-handwriting.koplugin` 整个文件夹；
3. 安全弹出，重启 KOReader。

**笔记数据不会随插件删除**。若也要清掉，需另行删除各书 `.sdr` 目录下的
`pencil_handwriting.lua`。

---

## 十、已知限制

- 不支持逐笔选中、改色、移动
- 橡皮擦为整笔删除，不能局部裁剪
- 无压感调宽
- **重排文档（EPUB / FB2）**：KOReader 不提供页面坐标系，笔画锚在屏幕坐标上，改动字号或版式后位置可能漂移
- 早期版本（v1，屏幕坐标）写下的笔迹在加载时被跳过，需要重画
- 绘制开启时笔无法操作菜单，需用指尖
- 墨迹极密的页面，翻页会变慢

---

## 附一、常用配置项（`pencilhw/config.lua`）

改动这些需要重新复制文件并重启 KOReader。默认值适用于绝大多数情况。

| 配置项 | 默认 | 作用 |
|---|---|---|
| `INPUT_SOURCE` | `"auto"` | 默认输入源（菜单里也能切） |
| `EXCLUSIVE_GRAB_DEFAULT` | `false` | 独占笔节点的初始状态，仅 evdev 路径生效 |
| `BLOCK_TOUCH_DEFAULT` | `false` | 初始是否屏蔽触摸 |
| `ERASER_RADIUS_PX` | `24` | 橡皮圆盘半径（屏幕像素） |
| `ERASE_MIN_MOVE_PX` | `4` | 橡皮移动多远才重新扫描一次 |
| `ERASE_MIN_INTERVAL_S` | `0.02` | 橡皮两次扫描的最小间隔 |
| `ERASE_REFRESH_MS` | `400` | 擦除过程中合并刷新的时间窗 |
| `REFRESH_SETTLE_MS` | `600` | 抬笔后多久做整屏刷新 |
| `REFRESH_GHOST_MS` | `8000` | 超长笔画中途清残影的间隔 |
| `PAGE_EXIT_CLEANUP` | `"on_ink"` | 离开有墨页面时是否整屏刷新（`"always"` = 每次翻页都刷） |
| `PAGE_EXIT_REFRESH_MODE` | `"full"` | 上述刷新使用的刷新模式 |
| `STORE_IN_PAGE_SPACE` | `true` | 笔画是否锚定页面坐标（关掉会退回屏幕坐标，不推荐） |
| `STROKE_FORMAT_VERSION` | `2` | 存储格式版本 |
| `DEFAULT_WIDTH` / `MIN_WIDTH` / `MAX_WIDTH` | `3` / `1` / `24` | 笔宽 |
| `DEFAULT_COLOR` | `"black"` | 默认颜色键 |
| `SIDECAR_FILENAME` | `"pencil_handwriting.lua"` | 数据文件名 |
| `EXTRA_DEVICE_PATHS` | `{}` | 优先尝试的输入节点，见 8.3 |
| `MENU_STRIP_RATIO` | `0.12` | 「屏蔽触摸」保留的顶部菜单条带高度比例 |

> `PAGE_KEY_SOURCE` 仅用于源码级调试：改成 `"live"` 会让笔画依据滞后的访问器归档，
> 可能把墨画到相邻页。**保持默认 `"auto"。**

---

## 附二、给贡献者的开发说明

- **私有命名空间**：所有子模块位于 `pencilhw/` 下。KOReader 的 `package.loaded` 是进程全局的，
  若使用 `core/store` 这类通用名，多个插件同时加载时会互相覆盖（先加载者胜出，后者静默拿到别人的模块），
  因此这里刻意做了命名空间隔离。
- **只加墨，从不铺白**：早期版本保留整屏白色画布并覆盖 framebuffer，结果盖住了页面与当时打开的菜单
  （表现为白屏），且 KOReader 一重绘笔记就消失。现在改为：新笔迹直接画进实时 framebuffer 局部刷新，
  并由 `ReaderView:paintTo` 钩子在每次重绘后补画已存笔画。
- **橡皮不能涂白**：同上原因，橡皮的语义是删除命中笔画 + 请求重绘。
- **坐标锚定**：笔画存**文档页面坐标**，用 KOReader 自己的 `screenToPageTransform` /
  `pageToScreenTransform` 实测映射（见 `pencilhw/viewmap.lua`）。滚动视图下每页各有偏移与缩放，
  因此映射**逐页求**，并与 reader 报告过的锚点对账；对不上时用锚点重建平移量而不是拒绝绘制。
- **健壮性约定**（都是踩过坑之后加的）：所有会被 KOReader 调用的回调都用 `pcall` 包裹，
  且出错时保持原有语义（手写笔回调出错也必须返回 `true` 以接管事件，否则笔会漏进手势识别）；
  NaN / Inf 在最外层拦掉，绝不进入脏区与刷新队列（NaN 矩形会让屏幕更新卡死）。
- **翻译**：菜单文本通过 `pencilhw/i18n.lua` 的词典跟随界面语言。新增菜单项记得补词条。

**开发期校验**：本机没有 Lua 解释器，插件目录外的 `.workbuddy/tools/lua_syntax_check.py`
用于静态检查——语法与括号平衡、`require` 路径是否存在（子模块 require 失败会让整个插件被丢弃）、
跨模块方法调用与 `Config` 键是否都有定义、`string.format` 实参是否够、`local function` 是否在声明前被调用、
以及 i18n 覆盖率与占位符一致性。改完代码建议跑一遍两种模式：

```bash
python3 .workbuddy/tools/lua_syntax_check.py <所有 .lua 文件>
python3 .workbuddy/tools/lua_syntax_check.py --refs <所有 .lua 文件>
```
